# מחשבון ציוני ספורט - בית הספר הריאלי

אתר לחישוב ציוני מבחני ספורט לתלמידי תיכון.

## הרצת האתר

**לשימוש בפועל, יש להעלות את כל הקבצים לשרת אינטרנט.**

## קבצי תצורה

האתר ניתן להרחבה מלאה על ידי עריכת קבצי CSV:

### 1. `options.csv` - אפשרויות בטפסים ומידע על מבחנים

קובץ זה מגדיר את כל האפשרויות בתפריטים הנפתחים ומידע על המבחנים:

```csv
field,value,label,description,input_format
grade,9,כיתה ט',,
grade,10,כיתה י',,
gender,male,זכר,,
gender,female,נקבה,,
test_type,2000m,ריצת 2000 מטר,הזן את הזמן בפורמט דקות:שניות (לדוגמה: 8:30),time
test_type,pullups,מתח,הזן את מספר החזרות שביצעת,count
test_type,4x10m,ריצת 4x10 מטר,הזן את הזמן בפורמט שניות (לדוגמה: 12.5),seconds
```

**העמודות:**
- `field` - סוג השדה (grade/gender/test_type)
- `value` - הערך הפנימי
- `label` - התווית בעברית
- `description` - הוראות למשתמש (רק למבחנים)
- `input_format` - סוג הקלט (רק למבחנים)

**ערכי `input_format` אפשריים:**
- `time` - זמן בפורמט דקות:שניות, נמוך יותר = טוב יותר (לדוגמה: 8:30)
- `seconds` - זמן בשניות, נמוך יותר = טוב יותר (לדוגמה: 12.5)
- `count` - מספר חזרות (מספרים שלמים), גבוה יותר = טוב יותר (לדוגמה: 20)
- `decimal` - ערכים עשרוניים, גבוה יותר = טוב יותר (לדוגמה: 2.5) - למרחקים, משך זמן, רמות

### 2. `scores.csv` - נתוני ציונים

קובץ זה מכיל את הסטנדרטים לציוני הספורט עבור כל קומבינציה:

```csv
test_type,gender,grade,top_score,bottom_score
2000m,male,9,8:30,10:30
2000m,female,9,10:00,12:30
pullups,male,9,15,6
```

**העמודות:**
- `test_type` - סוג המבחן (חייב להתאים ל-`test_type` ב-`options.csv`)
- `gender` - מגדר (`male` או `female`)
- `grade` - שכבת גיל (9, 10, 11, 12)
- `top_score` - הביצוע הנדרש לציון 100 (הטוב ביותר)
- `bottom_score` - הביצוע המינימלי לציון 55 (הגבול התחתון)

**חישוב הציון:**
הציון מחושב בצורה לינארית בין שתי נקודות:
- ביצוע שווה ל-`top_score` או טוב יותר = ציון 100
- ביצוע שווה ל-`bottom_score` או גרוע יותר = ציון 55
- ביצוע בין שתי הנקודות = ציון בין 55 ל-100 בצורה לינארית

הציונים תמיד יהיו בטווח 55-100, ללא חריגה מטווח זה.

## הוספת סוג מבחן חדש

לדוגמה, נוסיף מבחן "קפיצה לגובה":

1. הוסף ל-`options.csv`:
```csv
test_type,high_jump,קפיצה לגובה,הזן את הגובה במטרים (לדוגמה: 1.5),decimal
```

2. הוסף ל-`scores.csv` (עבור כל קומבינציה של מגדר ושכבה):
```csv
high_jump,male,9,1.5,1.0
high_jump,male,10,1.6,1.1
high_jump,female,9,1.3,0.9
high_jump,female,10,1.4,1.0
...
```

**הערה:** עבור כל מבחן, ודא ש:
- `top_score` מייצג ביצוע מצוין (ציון 100)
- `bottom_score` מייצג ביצוע מינימלי (ציון 55)
- המרחק בין שני הערכים הוא סביר ומייצג את טווח הביצועים הצפוי

זהו! המבחן החדש יופיע אוטומטית באתר.

## מבנה הקבצים

```
reali-sports/
├── index.html          # עמוד HTML ראשי
├── styles.css          # עיצוב האתר
├── calculator.js       # לוגיקת החישוב
├── options.csv         # אפשרויות בטפסים ומידע על המבחנים
├── scores.csv          # נתוני הציונים
└── README.md           # קובץ זה
```

## טכנולוגיות

- HTML5
- CSS3 (עיצוב מבוסס על אתר הריאלי)
- JavaScript (Vanilla JS, ללא תלויות)
- CSV לאחסון נתונים
